<div class="row">
  <div class="col-md-3">

    <h1>{{project.title}}</h1>

    <h2>Languages</h2>
    <div>
      <ul>
        {{#each languages as |language|}}
          <li>
              {{link-to language.id 'projects.project.language' project.id language.id}}
              <a href="#" class="btn btn-danger" {{action 'deleteLanguage' language.id}}>Delete</a>
          </li>
        {{/each}}
        {{add-language createLanguage="createLanguage"}}
      </ul>
    </div>
  </div>

  <div class="col-md-9">
    {{outlet}}
  </div>
</div>

